import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/projects/attention-feelings/demo');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

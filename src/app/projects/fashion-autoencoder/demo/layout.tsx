import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/projects/fashion-autoencoder/demo');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
